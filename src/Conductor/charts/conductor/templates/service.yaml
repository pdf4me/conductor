apiVersion: v1
kind: Service
metadata:
  name: {{ template "conductor.fullname" . }}
  labels:
    app: {{ template "conductor.name" . }}
    chart: {{ template "conductor.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "conductor.name" . }}
    release: {{ .Release.Name }}
