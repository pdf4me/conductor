apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pdf4me-conductor.fullname" . }}-test-connection"
  labels:
    {{- include "pdf4me-conductor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pdf4me-conductor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
